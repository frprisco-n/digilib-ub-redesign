
  # High-Fidelity Prototype Creation

  This is a code bundle for High-Fidelity Prototype Creation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
